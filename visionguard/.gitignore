# Secrets — never commit
.env
.env.local
*.pem
*.key

# Python
__pycache__/
*.pyc
.venv/
venv/

# Datasets & model artifacts (large / regenerable)
model-training/data/
model-training/models/
inference-server/models/*.onnx
inference-server/models/*.pt
runs/
*.log

# Node / Next
node_modules/
.next/

# MinIO local data
minio-data/
